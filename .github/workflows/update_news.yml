name: Mise à jour des actualités

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  update-news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Installer les dépendances
        run: npm install rss-parser node-fetch

      - name: Exécuter le script Node.js
        run: node fetch_news.js

      - name: Commit des modifications
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add actualites_completes.html
          git commit -m "🔄 Mise à jour automatique des actualités" || echo "rien à commit"

      - name: Push avec token personnel
        run: |
          git push https://x-access-token:${{ secrets.GH_PAT }}@github.com/FredericFsa/portfolio-fsa.git HEAD:main
